import React from 'react';

export default function SummerDecorations() {
  // Intentionally empty.
  // Summer decorations are anchored inside Header/Footer so they don't follow scroll.
  return null;
}
